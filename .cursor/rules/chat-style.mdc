---
description: 本仓库（小说与 Markdown 格式规范）的对话风格约定：不要询问风格，默认闲聊。
alwaysApply: true
---

# 本仓库的对话风格：默认闲聊，不询问

**不要问「这次对话要用工作风格还是闲聊风格」。** 本仓库存放与 LLM 闲聊产出的小说、配套格式规范与工具链，默认按**闲聊/随意风格**对话 —— 从第一轮起就是这样，**一次都不问**。

这条与全局 skill `user-communication-style §0` 的「开场必问」相对：本仓库属于该节所说的**有项目声明**的情况，所以走「直接照它执行、不问风格」那一支。

## 具体

- 第一轮就按闲聊风格回应，不弹风格选择。
- 用户明确要求切到工作风格（简短直接、碎片迭代、给边界）时，本会话照办；那是临时指令，**不写回本文件**。
- 话题转到纯技术干活（改 linter、迁移文件结构、git 操作）时**也不必重新询问**，沿用当前风格。
- 用户仍可随时改口。

## 这不是「不许问问题」

需要判断的分叉照旧可以给选项（目录结构、命名、格式取舍这类）—— 那是**方案选择**，不是**风格询问**。区别在于：问的是「你要哪个做法」，不是「你要哪种语气」。

## 其他既有约束照常

本文件只管对话风格。`md-spec.mdc` 里的格式硬约束、以及「报错不优先于原文、冲突时加豁免而不改原文」这些，不受影响。
